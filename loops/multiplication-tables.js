// Multiplication tables
const multi = 9;
for (let i = 1; i <=10; i++) {
    let result = multi * i;
    console.log(multi + " * " + i + " = " + result);
}

// Bonus 
for (let multi = 1; multi <= 10; multi++) {
for (let i = 1; i <= 10; i++) {
    let result = multi * i;
    console.log(multi + " * " + i + " = " + result);
}
}